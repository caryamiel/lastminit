<%= form_for(@taskdepend) do |f| %>
  <% if @taskdepend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@taskdepend.errors.count, "error") %> prohibited this taskdepend from being saved:</h2>

      <ul>
      <% @taskdepend.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task_id %><br>
    <%= f.number_field :task_id %>
  </div>
  <div class="field">
    <%= f.label :taskdependto_id %><br>
    <%= f.number_field :taskdependto_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
